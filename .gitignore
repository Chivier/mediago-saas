node_modules
*.local
.idea
.parcel-cache
*.tsno.mjs
.DS_Store
log
.turbo
.claude
.store
release
go.work.sum
apps/core/bin/
apps/electron/bin/
apps/main/bin/
.deps/

tmp/

# Python bytecode caches from the Python services (ai-service, follow-tracker).
__pycache__/
*.pyc

# Local planning notes that should not be published with the open-source repo.
docs/.vitepress/seo-keyword-map*.md
docs/.vitepress/private/

# SaaS deploy secrets (admin password, future API keys). Tracked .env
# was kept for upstream electron build defaults; new SaaS additions live
# here too, so we exclude it going forward.
.env
.env.local
